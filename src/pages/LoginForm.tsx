import React from 'react';
import { LoginFormm } from '@/components/login-form';
const LoginForm = () => {
  

  return (
     <>
     <LoginFormm></LoginFormm>
     </>
  );
};

export default LoginForm;
